{
  "name": "glory-simon-interiors-platform",
  "version": "1.0.0",
  "description": "Premium Interior Site Visit Booking & Project Management Platform for Glory Simon Interiors",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start --prefix backend\"",
    "start": "npm run dev",
    "seed": "node backend/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
